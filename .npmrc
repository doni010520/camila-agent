# pnpm 11+ requires explicit approval for native build scripts
# These deps have legitimate postinstall scripts (binary downloads / native compilation):
only-built-dependencies[]=@biomejs/biome
only-built-dependencies[]=esbuild
only-built-dependencies[]=msw
